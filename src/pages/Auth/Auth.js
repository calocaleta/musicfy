import React from 'react';
import { Button } from 'semantic-ui-react';
import "./Auth.scss"

const Auth = () => {
    return (
        <div>
            <Button primary>Auth Screen</Button>
        </div>
    );
};

export {Auth};